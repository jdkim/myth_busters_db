<%= form_for(@language) do |f| %>
	<% if @language.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@language.errors.count, "error") %> prohibited this language from being saved:</h2>

			<ul>
			<% @language.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
			</ul>
		</div>
	<% end %>

	<table class="project-properties">
		<tr>
			<th><%= f.label :label %></th>
			<td><%= f.text_field :label, required: true %></td>
		</tr>

		<tr>
			<th><%= f.label :iso639_3 %></th>
			<td><%= f.text_field :iso639_3 %></td>
		</tr>

		<tr>
			<th><%= f.label :autonyms %></th>
			<td><%= f.text_field :autonyms %></td>
		</tr>

		<tr>
			<th><%= f.label :glossonyms %></th>
			<td><%= f.text_field :glossonyms %></td>
		</tr>

		<tr>
			<th><%= f.label :reference %></th>
			<td><%= f.text_field :reference %></td>
		</tr>

		<tr>
			<th colspan="3"><%= f.submit %></th>
		</tr>
	</table>
<% end %>
