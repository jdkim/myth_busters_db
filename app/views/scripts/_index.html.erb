<div class='collections'>
	<%= render partial: 'script', collection: Script.order(:label) %>
	<% if current_user && current_user.root %>
		<%= link_to 'New', new_script_path, class: 'button new' %>
	<% end %>
</div>
