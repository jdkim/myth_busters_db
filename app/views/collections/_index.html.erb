<% content_for :index do %>
	<%= render partial: 'collection', collection: Collection.order(:label) %>
	<% if current_user && current_user.root %>
		<%= link_to 'New', new_collection_path, class: 'button new' %>
	<% end %>
<% end %>
